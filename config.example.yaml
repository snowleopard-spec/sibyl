paths:
  data_root: ./data

sec:
  user_agent: "Sibyl research your.name@email.com"
  rate_limit_per_sec: 8

universe:
  form_types: ["10-K", "10-Q"]
  include_amendments: false
  # Rolling 5y from today. Update on each `sibyl sp500 refresh`.
  history_start: "2021-06-20"

download:
  gzip: true
